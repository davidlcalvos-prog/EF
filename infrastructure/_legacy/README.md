# Infraestructura archivada (no mantenida)

Los manifiestos de AWS/EKS/Terraform y Kubernetes de esta carpeta quedaron fuera de uso en la Fase D.0.
El despliegue real del MVP es `infrastructure/docker/docker-compose.prod.yml` (VPS + Docker Compose + Caddy) — ver `infrastructure/docker/DEPLOY.md`.
Se conservan solo como referencia histórica; no se actualizan con el resto del repo.
