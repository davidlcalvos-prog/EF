/** Tokens alineados con apps/mobile/app/theme/eliteForgeColors.ts */
export const eliteForgeColors = {
  emerald: "#00CEC8",
  orange: "#FF8C00",
  carbon: "#424242",
  white: "#FFFFFF",
  carbonElevated: "#363636",
  carbonBorder: "#555555",
  carbonInput: "#2e2e2e",
  muted: "#9C9C9C",
  /** Texto oscuro legible sobre fondo emerald (solo web). */
  onEmerald: "#0B2E2C",
  /** Texto oscuro legible sobre fondo orange (solo web). */
  onOrange: "#2A1500",
} as const

/** Oro / plata / bronce para podios y rankings. */
export const medalColors = ["#FFD700", "#C0C0C0", "#CD7F32"] as const

/** Colores de marca de terceros (logos de login social); no son de la paleta EF. */
export const externalBrandColors = {
  googleBlue: "#4285F4",
  googleGreen: "#34A853",
  googleYellow: "#FBBC05",
  googleRed: "#EA4335",
} as const
