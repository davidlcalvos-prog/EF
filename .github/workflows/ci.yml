name: CI

on:
  push:
    branches: [main, develop, Dev-David]
  pull_request:
    branches: [main, Dev-David]

jobs:
  mobile:
    name: Mobile (React Native)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/mobile
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Instalar dependencias (monorepo)
        working-directory: .
        run: npm ci
      - name: Comprobar TypeScript
        run: npx tsc --noEmit -p . --pretty
      - run: npm run lint:check
      - run: npm test -- --passWithNoTests

  backend:
    name: Backend (NestJS)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Instalar dependencias (monorepo)
        working-directory: .
        run: npm ci
      - name: Comprobar TypeScript
        run: npx tsc --noEmit -p tsconfig.build.json
      - run: npm run build
      - run: npm test -- --passWithNoTests

  web:
    name: Web (Next.js)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Instalar dependencias (monorepo)
        working-directory: .
        run: npm ci
      - name: Comprobar TypeScript
        run: npx tsc --noEmit

  docker:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: [backend]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    strategy:
      matrix:
        service: [api-gateway, auth-service, users-service]
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build image
        run: |
          docker build \
            -f infrastructure/docker/Dockerfile.${{ matrix.service }} \
            -t ef-${{ matrix.service }}:latest \
            .
